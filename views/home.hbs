<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>some website</title>
  </head>
  <body>
    {{> header}}
    <p>
      {{welcomeMessage}}
    </p>
    <p>
      {{screamIt welcomeMessage}}   <!-- it uses the 'screamIt' helper passing welcomeMessage as parameter-->
    </p>
    {{> footer}}    <!-- use the "footer" partial file in "/partials" which is loaded by the following instruction in server.js
                         hbs.registerPartials(__dirname + '/views/partials'); -->
  </body>
</html>
